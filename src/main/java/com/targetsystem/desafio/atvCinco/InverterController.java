package com.targetsystem.desafio.atvCinco;

public class InverterController {

    public String one(){
        String inveter = "notrewe";
        String invertido = "";

        for (int i = inveter.length() -1; i >= 0; --i) {
            invertido += Character.toString(inveter.charAt(i));
        }
        return invertido;
    }

    public String two(){
        String inveter = "ibur";
        String invertido = "";

        for (int i = inveter.length() -1; i >= 0; --i) {
            invertido += Character.toString(inveter.charAt(i));
        }
        return invertido;
    }

    public String three(){
        String inveter = "rodamargorp";
        String invertido = "";

        for (int i = inveter.length() -1; i >= 0; --i) {
            invertido += Character.toString(inveter.charAt(i));
        }
        return invertido;
    }

    public String forr(){
        String inveter = "dnekcab";
        String invertido = "";

        for (int i = inveter.length() -1; i >= 0; --i) {
            invertido += Character.toString(inveter.charAt(i));
        }
        return invertido;
    }

    public String five(){
        String inveter = "roinuj";
        String invertido = "";

        for (int i = inveter.length() -1; i >= 0; --i) {
            invertido += Character.toString(inveter.charAt(i));
        }
        return invertido;
    }

    public String six(){
        String inveter = "tegraT";
        String invertido = "";

        for (int i = inveter.length() -1; i >= 0; --i) {
            invertido += Character.toString(inveter.charAt(i));
        }
        return invertido;
    }

    public String seven(){
        String inveter = "metsyS";
        String invertido = "";

        for (int i = inveter.length() -1; i >= 0; --i) {
            invertido += Character.toString(inveter.charAt(i));
        }
        return invertido;
    }
}
